ansible_user: ubuntu
ansible_become: true
ansible_ssh_private_key_file: ~/.ssh/id_rsa

project_name: bigdata-analysis
project_root: /opt/{{ project_name }}
controller_project_root: "{{ playbook_dir }}/../.."

compose_file: docker-compose.yml
compose_project_name: hadoop-spark
cluster_network_name: hadoop_network
